version: "3.8"

services:
  item-app:
    image: ghcr.io/cravingmaker/item-app:v1
    ports:
      - "80:8080"
    depends_on:
      - item-db
    restart: unless-stopped

  item-db:
    image: mongo:3
    volumes:
      - app-db:/data/db
    restart: unless-stopped

volumes:
  app-db:
